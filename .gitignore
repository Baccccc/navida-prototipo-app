# ==========================================================================
# NAVIDA — file da non versionare
# ==========================================================================

# Credenziali del database e parola d'ordine del team.
# Vive SOLO sul server. Su git c'e' solo il modello: impostazioni.esempio.php
#
# Se lo hai gia' committato per sbaglio, non basta questa riga: serve
#     git rm --cached api/impostazioni.php
# altrimenti ogni pull continua a sovrascrivere la versione del server.
api/impostazioni.php

# Dati salvati dal prototipo (se non si usa il database)
api/dati/
api/config.json
api/config.backup.json
api/storico/
navida-dati/
../navida-dati/

# Copie scaricate dalla barra laterale
navida-modifiche-*.json

# Roba di sistema
.DS_Store
Thumbs.db
desktop.ini
*.log

# Editor
.vscode/
.idea/
*.swp
*~
